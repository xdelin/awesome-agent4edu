version: "3.9"

services:
  streamlit-app:
    build: .
    container_name: multi-agent-assistant
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - .:/app
    restart: always
